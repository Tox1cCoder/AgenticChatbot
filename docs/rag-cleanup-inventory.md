# RAG cleanup inventory (Task 15)

Evidence supporting every removal and every retention decision made while
consolidating the RAG pipeline at HEAD `d6ef09a`. Per the plan's Global
Constraint, a path was removed only after a repository-wide caller search
proved it unreachable **and** its behavior (if any) was shown to already be
covered through the surviving replacement boundary. Where a repository grep
could not settle the question — a Pydantic setting, a Celery task name, a
frozen dependency manifest for an environment this checkout cannot install
into — the decision is **retain with a documented removal condition**, never
delete on the strength of a grep alone.

`app/ai/workflow/rag_loop.py` was out of reach for this task (it carries
another session's uncommitted work) and is not part of any row below except
where it is the reason a setting must be retained.

## Removed

| candidate | all_callers (pre-removal) | replacement | compatibility_reason | removal_risk | proof | decision |
|---|---|---|---|---|---|---|
| `RAGAgent._rerank_results` (`app/ai/agents/rag_agent.py`) | 3 test call sites only (`tests/test_rag_agent.py`, since removed); zero production callers | `RAGAgent._search` reranks typed `RetrievalCandidate` objects directly via `self.reranker.rank(query, candidates)` | Dict-shaped legacy adapter for callers that only had dict-shaped results; the one production search path (`execute_search_documents_action` in `app/ai/rag_tool_actions.py`) already calls `_search`, never `_rerank_results` | Low — the deleted test docstring read verbatim "Keep the legacy helper callable until its Task 15 removal gate," i.e. a prior task already flagged this as this task's job | `rg` for `_rerank_results` across `app/`, `tests/`, `scripts/`, `docs/` before removal showed only the definition and 3 tests, no caller in `rag_tool_actions.py`, `rag_loop.py`, `tool_loop.py`, `graph.py` | **Removed.** Reranking-through-`_search` behavior remains covered by `tests/test_rag_agent.py::test_rag_search_reranks_authorized_typed_pool_before_dict_adapter`, which already asserted `agent.reranker.rank.assert_awaited_once_with(...)` against the typed-candidate path; only the three tests exercising the dict adapter itself were removed. |
| `RAGAgent.get_document_full_content` (`app/ai/agents/rag_agent.py`) | 4 test call sites only (3 in `tests/test_rag_agent.py`, 1 in `tests/test_rag_multi_user_isolation.py`); zero production callers | `RAGAgent.get_document_chunk_window` (bounded, paginated, ownership-scoped via `DocumentChunkRepository.get_window_for_scope`) | Unbounded full-document read predates the bounded READ_DOCUMENT/SCAN_ALL windowing; `execute_search_documents_action`'s READ_DOCUMENT branch calls `get_document_chunk_window`, never this method | Low — several tests explicitly asserted `get_document_full_content.assert_not_awaited()` as a regression guard, confirming the production path already avoided it | `rg -n "get_document_full_content"` before removal showed 3 files: the definition, `tests/test_rag_agent.py`, `tests/test_rag_multi_user_isolation.py` — no hit in `rag_tool_actions.py`, `rag_loop.py`, `tool_loop.py`, `graph.py`, or any API route | **Removed.** Ownership-scoped bounded reads remain covered by `tests/test_rag_agent.py` chunk-window tests and by `tests/test_rag_document_scope_repository.py::test_chunk_windows_exclude_wrong_owner_and_wrong_conversation`, which exercises a real DB fixture (`rag_scope_db`) rather than mocks — stronger coverage than what was removed. |
| `DocumentParseService._create_chunks` + `from langchain_text_splitters import RecursiveCharacterTextSplitter` (`app/services/document_parse_service.py`) | Zero callers anywhere (production or test) | `DocumentNormalizer` (structural blocks) → `DocumentChunkBuilder.build()` (token-aware chunking) | Character-count chunker predates the token-aware, structure-preserving chunk builder that `parse_document`/`process_document` actually use | Low — no test ever called `_create_chunks` directly | `rg -n "_create_chunks\b"` (word-boundary, excluding the `def` lines) across `app/`, `tests/` returned zero call sites | **Removed.** Token-aware chunking behavior is covered throughout `tests/test_document_chunk_builder.py` and `tests/test_unified_parse_pipeline.py`. |
| `DocumentParseService._legacy_char_chunk_size`, `_legacy_char_overlap` (`app/services/document_parse_service.py`) | Only called from within `_create_chunks` and `_create_chunks_with_page_metadata`, both removed | N/A (chunk sizing is now `DocumentChunkBuilder`'s `target_tokens`/`overlap_tokens`/`max_tokens`) | Char-count heuristics (`~4 chars/token`) for the retired character chunker | Low — orphaned once their only two callers are gone | `rg -n "_legacy_char_(chunk_size\|overlap)"` showed no other reader | **Removed.** |
| `DocumentParseService._create_chunks_with_page_metadata` (`app/services/document_parse_service.py`) | 1 test call site (`tests/test_document_processing_service.py::test_mineru_content_list_table_body_is_indexed_as_searchable_text`, direct call on a `DocumentParseService` instance); zero production callers | `DocumentNormalizer.normalize_mineru()` (produces `NormalizedBlock`s with table caption/header/body/footnote) → `DocumentChunkBuilder.build()` | Page-metadata-preserving legacy chunker for MinerU `content_list.json` blocks; `_process_with_mineru` calls `document_normalizer.normalize_mineru(...)`, never this method | Medium before rewrite (one test directly depended on it), Low after — see Step 2 rewrite below | `rg -n "_create_chunks_with_page_metadata"` before removal showed only the definition (both files) and the one test call site | **Removed**, with its dependent test rewritten first (Step 2): `test_mineru_content_list_table_body_is_indexed_as_searchable_text` now chains `DocumentNormalizer().normalize_mineru(...)` into `_build_chunks_for_indexing(...)` — the actual production boundary — and still asserts the same table caption/body content reaches indexed chunk text. Additional coverage for the same invariant already existed in `tests/test_document_normalizer.py::test_table_retains_caption_header_body_and_footnote` and `tests/test_document_processing_service.py::test_build_chunks_for_indexing_recovers_table_body_from_parse_metadata`. |
| `DocumentProcessingService` orphaned parse-delegation wrappers: `_legacy_char_chunk_size`, `_legacy_char_overlap`, `_create_chunks`, `_extract_excel_rows`, `_excel_rows_to_markdown`, `_stringify_excel_cell`, `_escape_markdown_table_cell`, `_parse_content_list_json`, `_create_chunks_with_page_metadata`, `_normalize_filename_token`, `_collapse_filename_token`, `_build_filename_aliases`, `_resolve_mineru_output_dir`, `_resolve_markdown_file` (`app/services/document_processing_service.py`) | Zero callers anywhere — not even a test called the `DocumentProcessingService`-side wrapper (tests that exercised the underlying logic called it on a `DocumentParseService` instance directly) | `DocumentParseService` owns all parse logic; `DocumentProcessingService.process_document` only ever calls the two delegators that remain (`_process_excel_workbook`, `_process_with_mineru`) | The class's own docstring said these existed "so tests that call these methods directly keep working" — that never happened for these 14; only `_process_excel_workbook`/`_process_with_mineru` are exercised | Low — confirmed zero references anywhere in `app/`, `tests/`, `scripts/`, `docs/` beyond the definitions themselves | AST dump of `DocumentProcessingService`'s methods cross-referenced against a repository-wide `rg` for each name; `process_document`'s only two parse-stage calls are `self._process_excel_workbook(...)` and `self._process_with_mineru(...)`, both retained | **Removed** (14 methods). `_get_parse_service`, `_process_excel_workbook`, `_process_with_mineru` retained — `process_document` depends on them and `process_document` itself is out of scope for restructuring (see Retained table). |
| `DocumentProcessingService._attach_prepared_images_to_chunks` (`app/services/document_processing_service.py`) | Zero callers anywhere, including tests | `DocumentProcessingService._attach_prepared_images_to_blocks` (the `NormalizedBlock`-based version `process_document` actually calls) | Own docstring: "Version-1 compatibility helper for mutable chunk dictionaries" | Low | `rg -n "_attach_prepared_images_to_chunks\b"` returned only the definition | **Removed.** |
| `DocumentProcessingService._display_page_number` (`app/services/document_processing_service.py`) | Zero callers anywhere, including tests | N/A — no code path formats a page number this way anymore | Unused static helper | Low | `rg -n "_display_page_number"` returned only the definition | **Removed.** |

## Retained (documented compatibility shims / active paths)

| candidate | all_callers | replacement | compatibility_reason | removal_risk | proof | decision |
|---|---|---|---|---|---|---|
| `DocumentProcessingService.process_document` | Zero non-test callers (the Celery worker calls `_prepare_prepared_images`/`_persist_prepared_images`/`index_document` directly, per Task 11's Critical finding) | None shipped — the worker has not converged onto it | N/A | High if deleted: this is the only place implementing the plan-mandated persist-before-vector-writes ordering as one coherent method | Controller ruling in `.superpowers/sdd/2026-08-13-rag-production-hardening/task-15-context.md` | **Retained — out of scope for removal in this task**, per explicit controller instruction. Recommendation: converge the Celery worker onto `process_document` in a future task so this stops being a "zero-caller" method by naive count while remaining the reference implementation; do not delete it and do not restructure the worker to call it as part of a cleanup task. |
| `RAGAgent._search` | 1 production caller: `execute_search_documents_action` (`app/ai/rag_tool_actions.py:463`), plus extensive test coverage | N/A — this *is* the production search path | The brief's Step 3 says to delete `_search` "once tool actions consume typed services," but that migration has not happened — `rag_tool_actions.py` still calls `rag_agent._search(...)` for `SEARCH_CHUNKS` | Would break the only production RAG search path if removed | `rg -n "\._search\("` shows the live call site in `rag_tool_actions.py` | **Retained.** This is exactly the "no callers is the beginning of the analysis" trap in reverse — a naive reading of the brief's forward-looking phrasing would have deleted a live path. Not a cleanup candidate until tool actions are migrated to call `RAGRetriever` directly. |
| `settings.reranker_model` (`app/core/config.py`) | `scripts/download_reranker.py:41` reads it as a fallback (`getattr(settings, "rag_reranker_model", None) or settings.reranker_model`); `tests/test_rag_reranker.py:418` asserts default parity with `rag_reranker_model` | `settings.rag_reranker_model` (canonical; used by `RAGAgent`, `app/core/container.py`) | Backward-compat alias per its own Field description | Deleting it would raise `AttributeError` in `scripts/download_reranker.py` for any deployment whose environment sets the old `RERANKER_MODEL` var without also setting `RAG_RERANKER_MODEL` — unverifiable from this repository | `rg -n "reranker_model"` across `app/`, `tests/`, `scripts/`; confirmed `app/observability/rag.py`'s mention is a comment referencing `rag_reranker_model`, not a read of the alias | **Retained.** Removal condition: confirm (in an environment where deployed `.env` files can be inspected) that no environment sets `RERANKER_MODEL` without `RAG_RERANKER_MODEL`, then delete the Field from `config.py` and the fallback branch in `scripts/download_reranker.py` together. |
| `settings.enable_citation_verification`, `settings.min_citation_coverage`, `settings.rag_multimodal_image_embeddings_enabled` | Read in `app/ai/workflow/rag_loop.py` (protected — could not be edited this task), `app/ai/agents/rag_agent.py`, `app/core/container.py`; covered by `tests/test_rag_rollout_contract.py`, `tests/test_rag_tool_loop_finalization.py`, `tests/test_rag_multi_user_isolation.py`, `tests/test_rag_grounding.py`, `tests/test_retrieval_model_selection.py` | N/A — these are active feature flags, not dead code | Conservative rollout defaults per `docs/rag-rollout-runbook.md` | Deleting an active flag with real readers would be a behavior change, not cleanup | `rg` shows live reads in non-test code and extensive contract tests | **Retained — not a cleanup candidate.** These are exactly the kind of setting the Global Constraint says to treat as load-bearing until proven otherwise; they are proven *in use*, not dead. |
| `DocumentIndexService.reindex_document`, `set_payload` calls, `DocumentAction.SCAN_ALL` | `scripts/reindex_embeddings.py:151` (live CLI, has its own test suite `tests/test_reindex_embeddings_cli.py`); `execute_search_documents_action`'s SCAN_ALL branch | N/A — active production paths | N/A | Deleting either would break a shipped CLI tool or a supported agent action | `rg -n "reindex_document\|set_payload\|SCAN_ALL"` shows live callers, not orphaned aliases | **Retained — not a cleanup candidate.** (Note: a *different*, whole-document reparse CLI script was already fully removed in a prior task — `tests/test_production_readiness_contract.py::test_tracked_sources_and_docs_do_not_claim_a_document_reparse_cli` already enforces that no tracked file names it; this task found nothing further to do here.) |
| `ParseResult.__init__`'s `chunks_with_metadata=` v1 constructor kwarg (`app/services/document_parse_service.py`) | Zero production constructors use it (all construct with `blocks=`); 7 keyword-argument constructor calls in `tests/test_phase3_pipeline.py` (lines 65, 156, 221, 377, 450, 546, 617) | `ParseResult(blocks=...)` | "Accept the v1 constructor shape until all callers have rolled forward" | Medium — removing it means rewriting 7 test call sites in a file this task did not otherwise touch, for a 2-line compatibility branch with no proven production reader | `rg -n "chunks_with_metadata="` shows only these 7 test constructor calls (a further hit at that file's line 176 is a `.chunks_with_metadata` attribute read, not a constructor call, and is not counted here) | **Retained, not removed this pass.** Documented as a candidate for a future, narrowly-scoped pass: rewrite `tests/test_phase3_pipeline.py` to construct with `blocks=` (or via `DocumentNormalizer` output) first, then delete the compatibility branch. Note this is a distinct code path from `load_parse_result`'s `schema_version == 1` on-disk artifact handling, which is a persisted-data compatibility path and must not be removed regardless. |
| `langchain-text-splitters` dependency (`pyproject.toml` line 50, `requirements.txt` line 159) | No longer imported by any code in `app/` after this task's changes (`RecursiveCharacterTextSplitter` was the only user) | N/A | N/A | Unverifiable: per project knowledge, `requirements.txt`/`environment.yml` are a frozen snapshot for a *different* environment (Windows/py3.11/CUDA MinerU) than the `.venv` (py3.13) this task's tests run in, and cannot be installed or `pip check`-ed from this checkout. Removing the pin could be inert (if `langchain` itself pulls it in transitively) or could break a frozen/`--no-deps`-style install in that other environment | `rg` confirms zero remaining imports in `app/`, `tests/`, `scripts/` | **Retained — deployment-environment evidence unavailable.** Removal condition: in an environment where `pip install -r requirements.txt` (or the pyproject extras) can actually be exercised, confirm `langchain>=1.3.4` still resolves without an explicit `langchain-text-splitters` pin before dropping the line from both files in lockstep. |
| `app/ai/workflow/rag_loop.py` | N/A | N/A | Protected: holds another session's uncommitted work (the three-argument `_apply_hand_off_if_present` call) | Editing it risked destroying unrelated in-progress work | Explicit instruction in `task-15-context.md` | **Deferred entirely, not evaluated for removal candidates this task.** Any cleanup it needs is a future task's inventory row, not this one's. |
| `.env.example` | N/A | N/A | Guard-blocked for read/write by agents and reviewers | N/A | Explicit instruction in `task-15-context.md` | **No edit made or needed.** None of this task's removals touched an `app/core/config.py` `Settings` field (every settings-shaped candidate above was retained), so there is no corresponding `.env.example` key to remove. If a future pass does delete a `Settings` field, the repository owner must hand-merge the corresponding `.env.example` key since no session in this process — this one included — can diff it against the live file. |
| `scripts/benchmark_ingestion.py` vs. `scripts/benchmark_rag.py` overlap | Both have callers (`benchmark_rag.py` imports `benchmark_ingestion.py`'s helpers) | N/A | Higher-level aggregation duplicates across the two scripts | N/A — not dead code, a consolidation opportunity | Noted in `task-15-context.md`; confirmed `benchmark_rag.py` imports rather than re-implements | **Out of scope for this task.** Task 15's mandate and the Global Constraint are about removing *proven-dead* paths; this is two live scripts with duplicated aggregation logic, not a dead path. Left as a documented follow-up, not actioned, to avoid touching two working scripts without a test harness that exercises them (both require live Qdrant/Postgres). |
| `_record_stage`/`_record_cache_result` duplicated across `document_index_service.py`, `rag_retrieval.py`, `rag_reranker.py`, `rag_evidence.py`, `rag_grounding.py` | All five are live, tested call sites | A shared helper in `app/observability/rag.py` would collapse the duplication | N/A — duplicated but not dead | N/A | Noted in `task-15-context.md` | **Out of scope for this task.** This is a refactor (introduce a shared helper) rather than a dead-path removal, and touches five files' worth of tested behavior for a cosmetic win. Left as a documented follow-up. |
| `README.md`, `app/core/config.py`, `.env.example`, `requirements.txt`, `pyproject.toml` (brief's file list) | — | — | — | — | Verified: README's RAG/document-processing section (around line 696) already describes only the current `DocumentChunkBuilder`/token-aware path and never mentioned the retired character chunker, `_rerank_results`, or `get_document_full_content`; `config.py` has no field tied to any of this task's actual removals (every settings-shaped candidate was retained above) | **No changes made to these files.** Each was checked against every removal in this document; none required an edit because either (a) the file never documented the removed private methods in the first place, or (b) the corresponding settings/dependency candidate was retained rather than removed. Listing them here makes that verification explicit rather than silent. |

## Summary

- **8 dead code paths removed** (2 in `RAGAgent`, 3 unique chunking methods + shared helpers in `DocumentParseService`, and the `DocumentProcessingService` orphaned-delegator set of 14 methods plus 2 more dead helpers), each with a zero-caller repository-wide search and, where a test depended on the removed method, a rewritten test proving the same behavior through the replacement boundary first.
- **12 candidates evaluated and retained**, each with a documented reason and, where applicable, an explicit removal condition for a future task.
- **Zero `Settings` fields removed**, so `.env.example` requires no edit and none was attempted (it is also guard-blocked for this process regardless).
- `app/ai/workflow/rag_loop.py` was not evaluated for candidates; it was out of reach for this task per explicit instruction.
