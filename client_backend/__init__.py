"""
Client Backend for the Kani Desktop App.

This package provides a per-device local runtime that:
- Manages local tool execution (shell, filesystem)
- Manages local MCP server connections
- Manages local skill loading from device paths
- Proxies requests to the canonical server backend
- Handles device authentication and session management
"""

__version__ = "0.1.0"
