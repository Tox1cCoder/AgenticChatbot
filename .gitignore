# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/
ENV/
venv.bak/
.env
.env.client
.dev_secret_key

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log

# Misc
*.bak
*.tmp

# Storage (user uploaded content)
app/storage/document_images/
app/temp/

# Test output
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Build
build/
dist/
.desloppify/
*.egg-info/
.eggs/
WARP.md

# Others
Pipfile.lock
poetry.lock
tasksync
log.md
.traycer/
mcp_config.json
# The server's MCP config is the tracked global-tool allowlist (FR-3,
# enforced by tests/test_mcp_global_allowlist.py); machine-local MCP
# configs (sidecar profiles) stay ignored by the pattern above.
!app/ai/mcp_config.json
output/
boring_servers/
scripts/
.agent
.pytest_cache/
.ruff_cache/
.streamlit/
.vscode/
.claude
.codex_logs
.conda
.pytest_cache
.ruff_cache
# Anchored to repo root: ignore only the top-level personal skill-bundle
# directory. `shared/skills/` (importable shared code) and
# `tests/fixtures/skills/` (CI fixtures) are not matched and stay tracked;
# bytecode under them is still ignored by the __pycache__/ rule above.
/skills/
.codex_logs
.conda

# Legacy offloaded tool results (pre-Postgres blobs)
data/
storage/
